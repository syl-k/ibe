{
  "name": "ibe",
  "version": "0.0.0",
  "description": "Integrated browser + terminal with unlimited split panes (M0 prototype)",
  "license": "MIT",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc && npm run copy",
    "copy": "cp src/renderer/index.html src/renderer/styles.css dist/renderer/",
    "start": "npm run build && electron .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^33.2.0",
    "typescript": "^5.6.0"
  }
}
